---
# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Pull Request

on:
  merge_group:
  pull_request:

permissions:
  contents: read

jobs:
  changed:
    name: Discover changed sources
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    outputs:
      matrix: ${{ steps.matrix.outputs.matrix }}
      native: ${{ steps.native-files.outputs.changed_files != '[]' }}
      external: ${{ steps.external-files.outputs.changed_files != '[]' }}
    steps:
      - name: Get changed files
        id: changed-files
        uses: bjw-s-labs/action-changed-files@a9a36fb08ce06db9b02fbd8026cc2c0945eb9841 # v0.6.0
        with:
          patterns: |
            sources/**

      - name: Get changed native files
        id: native-files
        uses: bjw-s-labs/action-changed-files@a9a36fb08ce06db9b02fbd8026cc2c0945eb9841 # v0.6.0
        with:
          patterns: |
            native/**
            scripts/builtins-extract.sh

      - name: Get changed external files
        id: external-files
        uses: bjw-s-labs/action-changed-files@a9a36fb08ce06db9b02fbd8026cc2c0945eb9841 # v0.6.0
        with:
          patterns: |
            external/**
            scripts/external.sh

      - name: Build matrix
        id: matrix
        env:
          CHANGED_FILES: ${{ steps.changed-files.outputs.changed_files }}
        run: |
          matrix=$(jq -c '
            [ .[]
              | (split("/") | .[1:3]) as $p
              | {
                  name: ($p | join("/")),
                  dir:  ("sources/" + ($p | join("/"))),
                  out:  ("out/crds/" + ($p | join("__")) + ".yaml"),
                  artifact: ("crds-" + ($p | join("__")))
                }
            ] | unique_by(.name)
          ' <<< "$CHANGED_FILES")
          echo "matrix=$matrix" >> "$GITHUB_OUTPUT"

  build:
    if: ${{ needs.changed.outputs.matrix != '[]' }}
    needs:
      - changed
    name: Build ${{ matrix.source.name }}
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    strategy:
      matrix:
        source: ${{ fromJSON(needs.changed.outputs.matrix) }}
      fail-fast: false
      max-parallel: 10
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Mise
        uses: jdx/mise-action@9e7f7633ff6f6d6048a9418a68d48f288f50eb14 # v4.2.3
        with:
          experimental: true
          install_args: --locked

      - name: Restore vendor cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: out/vendor/${{ matrix.source.artifact }}
          key: vendor-${{ runner.os }}-${{ matrix.source.name }}-${{ hashFiles(format('{0}/vendir.yml', matrix.source.dir), format('{0}/kind.yaml', matrix.source.dir), format('{0}/extract.yaml', matrix.source.dir), 'scripts/build.sh', 'scripts/kind-extract.sh') }}

      - name: Extract CRDs
        env:
          VENDIR_GITHUB_API_TOKEN: ${{ github.token }}
          VENDOR_CACHE: ${{ github.workspace }}/out/vendor/${{ matrix.source.artifact }}
        run: mise run build "${{ matrix.source.dir }}" "${{ matrix.source.out }}"

  native:
    if: ${{ needs.changed.outputs.native == 'true' }}
    needs:
      - changed
    name: Build native schemas
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Mise
        uses: jdx/mise-action@9e7f7633ff6f6d6048a9418a68d48f288f50eb14 # v4.2.3
        with:
          experimental: true
          install_args: --locked

      - name: Build native schemas
        run: |
          mise run swagger
          crd-schema-publisher convert --openapi out/swagger.json --kustomize -o out/site

  external:
    if: ${{ needs.changed.outputs.external == 'true' }}
    needs:
      - changed
    name: Build external schemas
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Mise
        uses: jdx/mise-action@9e7f7633ff6f6d6048a9418a68d48f288f50eb14 # v4.2.3
        with:
          experimental: true
          install_args: --locked

      - name: Build external schemas
        env:
          VENDIR_GITHUB_API_TOKEN: ${{ github.token }}
        run: mise run external

  workflow-lint:
    name: Workflow Lint
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Setup Mise
        uses: jdx/mise-action@9e7f7633ff6f6d6048a9418a68d48f288f50eb14 # v4.2.3
        with:
          install: false

      - name: Resolve linter versions
        id: tools
        run: |
          echo "actionlint=$(mise config get tools.actionlint)" >> "$GITHUB_OUTPUT"
          echo "zizmor=$(mise config get tools.zizmor)" >> "$GITHUB_OUTPUT"

      - name: Lint workflows
        uses: home-operations/.github/actions/workflow-lint@69cad1e407df8070dd9cfc3314551e473544014e # workflow-lint-v1.0.3
        with:
          actionlint-version: ${{ steps.tools.outputs.actionlint }}
          zizmor-version: ${{ steps.tools.outputs.zizmor }}

  status:
    if: ${{ !cancelled() }}
    needs:
      - build
      - changed
      - external
      - native
      - workflow-lint
    name: Build Success
    runs-on: ubuntu-24.04
    permissions: {}
    steps:
      # cancelled counts as failed: !cancelled() above still runs this job when
      # individual needs were cancelled but the run was not, and a required job
      # that never reached a verdict must not clear the merge gate.
      - name: Any jobs failed?
        if: ${{ contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled') }}
        run: exit 1

      - name: All jobs passed or skipped?
        if: ${{ !(contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled')) }}
        run: echo "${{ toJSON(needs.*.result) }}"
